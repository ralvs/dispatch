import { PageSkeleton } from "@/components/ui";

export default function Loading() {
	return (
		<PageSkeleton title="Inbox" subtitle="Captured tasks without a home. Give each one a domain." />
	);
}
